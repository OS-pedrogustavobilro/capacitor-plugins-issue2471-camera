import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-plugins-issue2471-camera',
  webDir: 'www'
};

export default config;
